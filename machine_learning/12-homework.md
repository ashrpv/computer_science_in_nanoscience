# Домашнее задание. Выбор метода и следующего эксперимента

Практика к [лекции 12](12-ml-methods_and_experiment.md) «Машинное обучение: выбор метода и следующего эксперимента». Задачи продолжают тему квантовых точек и нанокомпозитов, но учебные данные **искусственные**. В отдельном опыте допустимо использовать свои спектры, если подготовлены партии, паспорт съёмки и разбиение до настройки модели.

```python
import numpy as np
import pandas as pd

rng = np.random.default_rng(120)
wl_nm = np.arange(500, 601, 10)
rows, spectra = [], []
for campaign in range(8):
    campaign_shift = rng.normal(0, 2)
    for batch in range(6):
        polymer_pct = float((campaign + batch) % 4)
        temp_c = 75 + 10 * ((2 * campaign + batch) % 4)
        peak_nm = 540 + 2.2 * polymer_pct + 0.06 * (temp_c - 90)
        curve = (0.10 + np.exp(-0.5 * ((wl_nm - peak_nm) / 16)**2)
                 + rng.normal(0, 0.025, wl_nm.size))
        retention = (63 + 3.2 * polymer_pct - 0.14 * abs(temp_c - 90)
                     + 5 * curve.max() + campaign_shift + rng.normal(0, 2))
        rows.append((f"C{campaign}-B{batch}", campaign, polymer_pct,
                     temp_c, retention))
        spectra.append(curve)
table = pd.DataFrame(rows, columns=["batch_id", "campaign_id", "polymer_pct",
                                    "temp_c", "retention_pct"])
X_spectra = np.asarray(spectra)
```

Каждая строка — независимо изготовленная партия нанокомпозита. Спектр зарегистрирован **до** испытания сохранения фотолюминесценции через неделю. Сетка `wl_nm` и строки `X_spectra` имеют одинаковый порядок. Храните `batch_id` при каждом преобразовании массива.

## 🔎 Блок 1. Сравнение методов

### 1. Постановка и удержанная проверка

**Контекст:** Нужно прогнозировать `retention_pct` до недельной выдержки, используя состав, температуру и первичный спектр.

**Задача:** Удержите кампанию 7 для финального теста, настройку проводите на 0–6 с помощью групповой проверки по `campaign_id`. Проверьте отсутствие пересечения партий и кампаний между частями. Назовите метрику в процентных пунктах, baseline и допустимую ошибку до обучения. Почему случайная разбивка спектральных точек одной партии бессмысленна?

### 2. Лестница моделей

**Контекст:** Доступно всего 42 независимые партии для обучения.

**Задача:** На одних и тех же складках сравните `DummyRegressor`, Ridge по `polymer_pct` и `temp_c`, и Ridge по этим двум признакам плюс спектральные каналы. Включите масштабирование в `Pipeline`, а подбор регуляризации — внутрь групповой проверки. Запишите MAE для складок и итоговую MAE на удержанной кампании; объясните, почему один удачный тест из шести партий ещё не обеспечивает устойчивого качества.

### 3. PLS для коррелированных каналов

**Контекст:** Соседние спектральные отсчёты сильно коррелируют.

**Задача:** Обучите PLS-регрессию на спектрах с числом компонент 1–4; выбирайте его только внутри обучающей части и проверяйте, что число компонент допустимо на каждой складке. Сравните с моделью Ridge задачи 2. Покажите одну ошибку, которая возникнет при вычислении масштаба или PLS-компонент на всех партиях до разбиения.

### 4. PCA и структура набора

**Контекст:** PCA помогает увидеть вариацию спектров, но не гарантирует разделения технологических групп.

**Задача:** Обучите PCA только на тренировочных спектрах, спроецируйте отложенную кампанию и раскрасьте точки по содержанию полимера. Подпишите доли объяснённой дисперсии. Объясните, почему ось PC1 может отражать яркость или дрейф прибора, а визуальное разделение не доказывает причинного влияния полимера.

## 🧪 Блок 2. Решение для лаборатории

### 5. Дерево, важность и область применимости

**Контекст:** Дерево может уловить пороговые эффекты, но легко переобучается на небольшой выборке.

**Задача:** Сравните неглубокое дерево, случайный лес и градиентный бустинг с лучшей простой моделью на **тех же** складках. Для леса заранее ограничьте `min_samples_leaf`; для бустинга задайте небольшую глубину и сравните хотя бы две заранее выбранные пары (`learning_rate`, `n_estimators`). Встроенную случайную раннюю остановку отключите либо обеспечьте групповой способ её оценки. Запишите MAE **по каждой кампании**: увеличение числа деревьев само по себе не увеличивает число независимых партий. Для интерпретации один раз вычислите перестановочную важность выбранной модели на удержанной кампании и отметьте сильную корреляцию соседних спектральных каналов; почему отдельная важность канала не доказывает его физическую роль? Назовите диапазоны температуры и состава, вне которых прогноз не проверен.

**Ручной расчёт:** возьмите четыре учебных опыта лекции: температуры 80, 85, 95, 100 °C и ответы 4, 6, 10, 12. Найдите константу $F_0$, остатки, средние остатки по сторонам порога 90 °C и предсказание после **одного** дерева при $\eta=0{,}5$. Рассчитайте MAE до и после шага (в условных единицах). Объясните, что бустинг будет делать на втором шаге и чем процедура отличается от усреднения независимо обученных деревьев леса.

### 6. Устойчивость кластеризации

**Контекст:** Исследователь хочет найти «типы» спектров без меток.

**Задача:** На спектрах тренировочных партий после масштабирования сравните кластеризацию `k=2` и `k=3`. Повторите её на нескольких подвыборках **партий**, сопоставьте устойчивость разметки по общим объектам и сохраните хотя бы один показатель (например, ARI). Не подбирайте число кластеров по удержанному тесту и не называйте кластеры фазами вещества без дифракционного или иного независимого подтверждения.

### 7. Следующие три синтеза

**Контекст:** Есть ресурсы на три новые независимые партии. Допустимая область: полимер 0–3 %, температура 75–105 °C. Цель — уточнить модель для будущих партий, а не только повысить её оценку на уже известных данных.

**Задача:** Предложите три сочетания факторов: одно для проверки участка с высоким ожидаемым сохранением, одно в слабо представленном допустимом участке и одно подтверждающее повторение. Для каждого укажите научную причину, прогноз с оговоркой о неопределённости, критерий успешной проверки и порядок рандомизации. Спектр будущей партии нельзя использовать для выбора условий синтеза **до** его получения: отделите решение «что синтезировать» от решения «какую партию испытывать неделю».

## 🔬 Блок 3. Реальные материалы: один трек на выбор

Выберите трек **A** или **B**. На каждом шаге фиксируйте версию и источник файлов, состав признаков, единицу наблюдения, заранее выбранный тест и критерий полезности. Если Kaggle требует входа или исходные файлы старого ноутбука недоступны, сдайте воспроизводимый план работы на исходном наборе и выполните обязательный аудит кода из блока 4; не подменяйте недоступные результаты вымышленными метриками.

### Трек A. RGB → pH

Возьмите [pH-recognition](https://www.kaggle.com/datasets/robjan/ph-recognition). Автор формулирует задачу как прогноз pH по цвету RGB; **таблица RGB не является архивом полных фотографий**. Сначала откройте файлы и документируйте фактические имена столбцов и число строк. Проверьте пропуски, допустимые диапазоны R/G/B, распределение pH и возможные дубликаты троек RGB. Не считайте совпадающие строки независимыми измерениями автоматически.

Сравните `DummyRegressor`, Ridge, неглубокий градиентный бустинг и, по желанию, `MLPRegressor(hidden_layer_sizes=(8,))` с приведением RGB к масштабу *внутри* обучающих складок. Покажите MAE в единицах pH, график «факт–прогноз» и ошибки по интервалам pH. Подберите разумный малый список настроек на разработке и только после выбора откройте тест. Если в исходных данных нет ID раствора/съёмки/прибора, честно подпишите случайную проверку как проверку на похожих записях и **спроектируйте** отдельную внешнюю серию: другие независимо приготовленные растворы, отдельная сессия с контролем света и эталонный pH-метр. Нельзя утверждать, что хорошая ошибка на случайных строках подтверждает работу на новом телефоне.

### Трек B. Молекулы → липофильность

Изучите [Physical Chemistry – Lipophilicity](https://www.kaggle.com/code/mmelahi/physical-chemistry-lipophilicity) и доступный к нему источник данных. Цель ноутбука — экспериментальный $\log D$ при pH 7,4; при отсутствии файлов указанного автором расположения можно использовать исходный набор Lipophilicity из [MoleculeNet](https://doi.org/10.1039/C7SC02664A), указав изменение источника и схемы. Сохраняйте связь дескрипторов со структурой/ID молекулы. Сравните константу, Ridge и ограниченный бустинг по одной схеме. Для выборки, предназначенной для новых химических серий, предложите проверку с разделением близких структур по каркасу или семейству, если есть достоверные SMILES; для случайного деления отдельно напишите, что именно оно оценивает. В качестве необязательного расширения обучите небольшую сеть на тех же тренировочных/тестовых молекулах. Покажите RMSE и MAE в единицах $\log D$ и сравнение по области применимости, а не только одну лучшую цифру.

## 🧭 Блок 4. Аудит опубликованного ноутбука и обзор последовательного решения

### 8. Где «увидели» тестовые молекулы?

Откройте ячейки ноутбука по липофильности с `StandardScaler`, `KernelPCA`, `train_test_split` и последующей сборкой `data = np.concatenate((X_train, X_test), axis=0)`. Укажите **две** точки, где признаки теста влияют на `fit`, хотя метка `y_test` туда не передавалась. Перепишите порядок действий словами или коротким псевдокодом так, чтобы импутация, масштабирование, уменьшение размерности и настройка моделей обучались только на train каждой внутренней складки. Объясните, почему после просмотра результатов нескольких моделей на одном тесте нельзя многократно менять систему и продолжать считать этот тест независимой оценкой.

Если в ноутбуке встречается название «Gradient Boosting of Keras Model with SVC», посмотрите фактический класс модели в соответствующей ячейке. Разделите *использование представления нейросети с последующим SVR* и *градиентный бустинг деревьев*: одинаковое слово в заголовке не делает алгоритмы тождественными.

### 9. Что потребовалось бы для управления процессом?

Для выбранной научной установки опишите одно доступное онлайн состояние, одно допустимое действие, источник следующего наблюдения, отложенное вознаграждение и жёсткое ограничение. На условных числах лекции при $\gamma=0{,}9$ пересчитайте суммарную ценность быстрого и плавного нагревания. Объясните, почему таблицы RGB → pH и молекула → $\log D$ не дают траекторий действий для обучения политики и как вы сначала проверили бы политику, прежде чем применять её к установке. Код агента RL не требуется.

## Что сдать и как оценивать

Notebook с неизменяемым удержанным тестом синтетического блока, таблицей моделей и ошибок по кампаниям, ручным шагом бустинга, рисунком PCA, таблицей устойчивости кластеров и протоколом трёх следующих синтезов. Для **одного выбранного** реального трека сдайте таблицу источника/столбцов/разбиения/результатов или обоснованный план при недоступных файлах; добавьте аудит ноутбука и описание последовательной задачи. Код нейронной сети и RL необязателен.

Критерии: проверка по независимым единицам и отсутствие утечек — 30%; сравнение методов и объяснение шага бустинга — 25%; исследовательское решение и план синтезов — 20%; реальный трек с честной трактовкой пределов проверки — 15%; аудит ноутбука и постановка последовательного управления — 10%. Сложная модель без корректного теста не даёт зачёта за проверку.
