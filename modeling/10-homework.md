# Домашнее задание. Научное и стохастическое моделирование

Практика к [лекции 10](10-scientific_and_stochastic_modeling.md). Все численные данные ниже **искусственные**; можно подставить свои измерения сорбции, выщелачивания, высвобождения вещества или агрегативной устойчивости, описав исходный протокол. Для каждого расчёта разделяйте проверку кода, оценку параметров и сравнение модели с независимым опытом.

## ⚙️ Блок 1. Модель и параметры

### 1. Баланс в опыте по выщелачиванию

**Контекст:** В сосуде с обратной микроэмульсией концентрация растворённой меди `C` в ммоль/л приближается к пределу `C_inf`. Для учебного описания предложена модель `dC/dt = k(C_inf − C)` при `C(0)=0`, где время измеряется в минутах.

**Задача:** Выведите размерность `k`, аналитическое решение и укажите границы применимости модели. Проверьте единицы в каждом члене ОДУ. Запишите, чем физическая масса растворённой меди отличается от измеряемой концентрации, если объём раствора меняется.

### 2. Численный решатель против известного ответа

**Контекст:** Для проверки кода берём `k = 0.06 мин⁻¹`, `C_inf = 4.0 ммоль/л` и сетку времени от 0 до 60 мин.

**Задача:** Решите ОДУ через `scipy.integrate.solve_ivp`, сравните с аналитическим решением на общей сетке и сообщите максимальное абсолютное отклонение. Уменьшите `rtol` и `atol` минимум на порядок и оцените устойчивость рассчитанного `C(60)`; не называйте число точек `t_eval` шагом внутреннего интегратора.

### 3. Подгонка и остатки

**Контекст:** Учебные измерения концентрации (ммоль/л) на временах `[0, 5, 10, 20, 40, 60]` мин равны `[0.00, 0.95, 1.75, 2.78, 3.67, 3.87]`.

**Задача:** Оцените `k` и `C_inf` нелинейной подгонкой при условии `k > 0`, `C_inf > 0`. Покажите исходные точки, подгонку и отдельный график остатков, укажите критерий минимизации. Сравните прогноз с линейной моделью `C = a + bt` по остаткам и физическим ограничениям; не делайте вывод о превосходстве по одному только значению R².

### 4. Идентифицируемость при коротком эксперименте

**Контекст:** При наблюдении только первых 10 минут предел `C_inf` плохо определяется: разные пары `k`, `C_inf` могут давать сходные начальные наклоны.

**Задача:** Повторите подгонку на первых трёх точках и сравните параметры с задачей 3. Постройте профиль ошибки: фиксируйте сетку значений `C_inf`, для каждого оптимизируйте `k`. Укажите область почти равных ошибок и предложите новые времена измерения для разделения параметров. Объясните, почему формальная матрица ковариации на трёх точках может давать чрезмерно уверенное впечатление.

## 🎲 Блок 2. Неопределённость и случайные процессы

### 5. Распространение неопределённости

**Контекст:** Для иллюстрации, независимо от оценок задачи 3, предположим `k ~ N(0.06, 0.006²)` мин⁻¹ и `C_inf ~ N(4.0, 0.20²)` ммоль/л. Физически недопустимые отрицательные реализации нужно обработать по явно заданному правилу.

**Задача:** С фиксированным seed проведите не меньше 5000 реализаций и получите распределение `C(30)` и `t_90 = ln(10)/k`. Покажите медиану и интервал 2.5–97.5 % с единицами. Подсчитайте долю недопустимых реализаций и объясните, почему этот интервал описывает **принятую входную неопределённость**, а не автоматически неопределённость оценок из задачи 3.

### 6. Случайное блуждание как модель транспорта

**Контекст:** Частица биологически активного вещества диффундирует внутри модельного носителя в одном измерении. Для учебной дискретной модели шаг `Δt = 0.01 с`, коэффициент `D = 0.5 мкм²/с`, а приращение `Δx` нормально с дисперсией `2DΔt`.

**Задача:** Смоделируйте не меньше 2000 независимых траекторий за 2 с с фиксированным seed, постройте несколько траекторий и средний квадрат смещения по времени. Сравните его с теорией `MSD(t) = 2Dt` и оцените D по линейному участку. Повторите при половинном шаге времени и объясните, что изменилось из-за конечного числа траекторий и дискретизации. Укажите, почему одни и те же частицы, снятые многократно, не равны независимым синтезам носителя.

### 7. Проверка на новом опыте

**Контекст:** Подгонка модели по одному эксперименту не является подтверждением её предсказательной силы.

**Задача:** Выберите один из девяти научных направлений и запишите модель с наблюдаемой величиной, параметрами, единицами и областью применения. Предложите независимый опыт или новые партии для проверки **заранее записанного** предсказания; задайте метрику расхождения и допустимую границу до получения новых данных. Для учебной кинетики задачи 3 можно предложить новый опыт с повторными независимыми сосудами и заранее выбранными временами. Отделите источники разброса «между партиями», «измерение» и «численный расчёт».

## Что сдать

Исполняемый notebook или `.py`-файл с фиксированными seed, рисунками решения/остатков/профиля/МСD, таблицей оценок с единицами и коротким протоколом независимой проверки. Отдельно перечислите, какие результаты являются проверкой кода, а какие — лишь условными выводами из искусственных данных.
