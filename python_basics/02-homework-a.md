**1. Моделирование стоп-зоны фотонного кристалла**

При темплатном синтезе опалов важно быстро оценивать положение фотонной запрещенной зоны (стоп-зоны). По закону Брэгга-Снеллиуса для нормального падения света длина волны отражения рассчитывается как λ = 2 · d · n_eff, где d — диаметр сфер полистирола (нм), а n_eff — эффективный показатель преломления.

Напишите функцию `calculate_stop_zone`, которая принимает аргументы `d` и `n_eff` и возвращает длину волны λ. Вызовите функцию для сфер диаметром d = 230 нм и n_eff = 1.45. Выведите результат на экран.

Функция должна только возвращать значение (return), но ничего не печатать сама.

**2. Калибровка потенциостата при выщелачивании меди**

В программе управления потенциостатом для контроля кинетики выщелачивания меди в обратных микроэмульсиях задан глобальный фоновый ток ячейки: `BACKGROUND_CURRENT = 0.005` (мА).

Напишите функцию `adjust_signal(measured_current)`. Внутри функции:

1. Создайте локальную переменную `noise = 0.001`.
2. Попробуйте перезаписать глобальную переменную: `BACKGROUND_CURRENT = 0.008` (без ключевого слова `global`).
3. Функция должна возвращать чистый сигнал: `measured_current - BACKGROUND_CURRENT - noise`.

Вызовите функцию для значения 0.050, а затем выведите на экран значение `BACKGROUND_CURRENT` в основном коде. Объясните: «Почему значение фонового тока вне функции не изменилось?»

---

**3. Паспорт синтеза перовскитных квантовых точек**

Лабораторный журнал автоматически генерирует отчет о синтезе нанокомпозитов на основе квантовых точек CsPbBr₃.

Напишите функцию `log_perovskite_synthesis(halide, temperature, polymer)`. Она должна возвращать строку вида: `"Квантовые точки состава CsPb{halide}3 интегрированы в матрицу {polymer} при {temperature}°C."` Вызовите эту функцию двумя способами:

1. Передав строго позиционные аргументы (`"Br"`, 120, `"ПММА"`).
2. Передав именованные (keyword) аргументы, специально нарушив их исходный порядок (например, начав с `polymer`).

**4. Стандартный протокол подготовки наноэмульсий**

Большинство тестов агрегативной устойчивости наноэмульсий персикового и облепихового масел проводятся по стандартному протоколу: время гомогенизации — 10 минут, частота ультразвука — 20 кГц, в присутствии сквалена как стабилизатора.

Напишите функцию `prepare_emulsion(oil_type, time_min=10, frequency_khz=20, surfactant="Сквален")`. Функция должна возвращать словарь с параметрами эксперимента. Вызовите её тремя способами:

1. Указав только вид масла (`"Абрикосовое"`).
2. Изменив только время гомогенизации на 25 минут.
3. Полностью переопределив все параметры (например, для лецитиновой микроэмульсии).

**5. Ошибка в конфигурации индентора**

Студент пишет скрипт для расчета модуля упругости эпокси-аминных сеток по кривым наноиндентации, но допускает синтаксическую ошибку в объявлении параметров.

Исправьте ошибку в коде ниже, не удаляя параметры, и сформулируйте правило Python, которое было нарушено:

```python
# Данный код вызовет ошибку SyntaxError при запуске. Исправьте его.
def setup_indenter(tip_geometry="Berkovich", max_load, hold_time=5, poisson_ratio):
    return f"Настройка: тип иглы {tip_geometry}, нагрузка {max_load} мН, выдержка {hold_time} с"
```

**6. Средний размер твердых липидных наночастиц**

При динамическом светорассеянии прибор делает несколько последовательных измерений гидродинамического радиуса липосом (их может быть 3, 5 или 10 в зависимости от стабильности системы).

Напишите функцию `get_average_particle_size(*args)`, которая принимает любое количество измеренных радиусов (как позиционные аргументы). Функция должна возвращать среднее арифметическое.

Если прибор дал сбой и функция вызвана без аргументов вовсе (`get_average_particle_size()`), она должна возвращать `0.0`, а не падать из-за деления на ноль.

**7. Профиль сорбции пористого полимера**

Высокопористые полимерные сорбенты на основе стирола испытываются на разделение водонефтяных эмульсий. Каждый образец имеет уникальный набор физико-химических свойств, передаваемых в базу данных.

Напишите функцию `describe_sorbent(sample_id, **kwargs)`. Функция должна выводить на экран строку `"Анализ образца: [sample_id]"`, а затем с помощью цикла по `kwargs.items()` распечатывать все переданные характеристики (например, `pore_volume=0.8`, `magnetic=True`, `surface_area=450`) в формате: `"- [Свойство]: [Значение]"`.

**8. Комплексный логгер фазового поведения лецитина**

Для построения тройных фазовых диаграмм жидкокристаллических систем «лецитин — масло — вода» создается универсальная функция сбора данных.

Напишите функцию `register_phase_system(operator, *components, state="Жидкие кристаллы", **fractions)`. Она должна принимать:

- Имя исследователя (`operator`).
- Любое количество присутствующих веществ (`*components`).
- Текущее фазовое состояние как параметр по умолчанию (`state`).
- Массовые доли компонентов в виде именованных параметров (`**fractions`).

Функция должна возвращать структурированный словарь или строку со всеми этими данными. Продемонстрируйте её вызов.

---

**9. Экспресс-анализ сорбционной емкости**

Получен массив данных по сорбции предельных углеводородов внутри пор полимерных нанокомпозитов (в граммах поглощенного вещества на грамм сорбента): `sorption_data = [4.2, 5.1, 3.8, 4.2, 6.0, 3.9, 5.1]`.

Не используя циклы (`for`, `while`), с помощью встроенных функций Python:

1. Определите максимальное и минимальное значения сорбции.
2. Рассчитайте среднюю сорбционную емкость по формуле `sum() / len()`.
3. Создайте из списка множество (`set`), чтобы узнать количество уникальных результатов измерений, и выведите его длину.

**10. Картирование фотокатализа наночастиц NiO**

Изучаются газочувствительные свойства 2D-максена и иерархических структур оксида никеля. Экспериментатор сопоставляет время УФ-облучения образца со степенью деградации загрязнителя.

Даны два списка равной длины: `time_steps = [0, 15, 30, 45]` (минуты) и `efficiency = [0.0, 42.5, 78.1, 95.4]` (процент разложения).

1. С помощью `zip` объедините их в пары.
2. Используя цикл `for` и функцию `enumerate`, пронумеруйте шаги и выведите результат на экран строго в виде:

```
"Точка 0: Время = 0 мин, Эффективность = 0.0%"
"Точка 1: Время = 15 мин, Эффективность = 42.5%"
...
```