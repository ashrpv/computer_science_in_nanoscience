# Домашнее задание №1. Основы Python и структуры данных

Начните с задач 2–5 и 7 после лекции 1. Задачи 1, 6, 8, 9 и 10 используют списки, словари или множества из лекции 2. Все численные пороги и данные ниже учебные; они не задают универсальных свойств материалов. Решение должно запускаться на Python 3.11 или 3.12 без сторонних библиотек.

**Что сдать:** один notebook или несколько `.py`-файлов с решениями и короткими ответами на вопросы в задачах. Покажите контрольные случаи для границ размера в задаче 3 (`0`, `10`, `50` нм), условия допуска в задаче 4 и остановки цикла в задаче 5. Сохраняйте исходные измерения отдельно от результатов проверки качества.

## 🔬 Блок 1: Типы данных и базовые операции

### 1. Коды нанотрубок (Строки и списки)

**Контекст:** При составлении учебного списка образцов углеродных нанотрубок используются условные внутренние коды. Такие коды не являются индексами хиральности: хиральность нанотрубки задают парой индексов $(n,m)$.

**Задача:** Дана строка `tubes = "CNT_10-CNT_20-CNT_30"`.

1. Разбейте эту строку на отдельные элементы (по дефису).
2. Выведите на экран фразу: `"Коды образцов нанотрубок: [список кодов]"`.
3. Умножьте количество элементов в полученном списке на длину исходной строки `tubes` и выведите результат. Что за тип данных получился?

---

### 2. Масштаб атома (Преобразование типов и округление)

**Контекст:** Один нанометр равен $10^{-9}$ метра. Учебная программа принимает размер в метрах и округляет результат до 11 знаков после запятой; это правило форматирования не повышает точность измерения.

**Задача:** Создайте переменную `size_nm = 45`. Переведите это значение в метры ($45 \times 10^{-9}$). Округлите результат до 11 знаков после запятой с помощью функции `round()`. Выведите тип итоговой переменной.

---

## 🧪 Блок 2: Условный оператор (if-elif-else)

### 3. Категории размеров наночастиц (Условия)

**Контекст:** Для учебного каталога выбрано условное правило классификации частиц по диаметру: меньше 10 нм — «малые», от 10 до 50 нм включительно — «средние», больше 50 нм — «крупные». По одному диаметру нельзя назначить цвет золотой наночастицы: на оптический отклик влияют также форма, окружение и агрегация.

**Задача:** Для заданной числовой переменной `size` напишите программу, которая выводит условную категорию размера.

- **Защита от ошибок:** Если пользователь случайно ввел отрицательное число или ноль, программа должна вывести: `"Ошибка: размер не может быть меньше или равен нулю"`.

---

### 4. Проверка учебного допуска (Логические операторы)

**Контекст:** В учебном алгоритме переход к следующему этапу разрешён только при температуре от 500 до 800 °C включительно, давлении строго выше 2 атм и отсутствии избытка кислорода (`oxygen_excess = False`). Это заданные для упражнения условия допуска, а не физический критерий устойчивости фуллерена или инструкция по работе с реактором.

**Задача:** Задайте три переменные с тестовыми значениями. Напишите одно составное условие с использованием `and` и `not`, которое выводит `"Параметры в учебном допуске"`, если все условия соблюдены, и `"Проверьте параметры"` в противном случае. Проверьте оба граничных значения температуры и точное равенство давления 2 атм.

---

## 🔄 Блок 3: Циклы (for и while)

### 5. Рост нанокристалла (Цикл while)

**Контекст:** В условной численной модели объём кристалла каждую секунду удваивается. Начальный объём — 1 нм³, учебный предел — 1000 нм³. Это упражнение на цикл, а не модель реальной эпитаксии.

**Задача:** Напишите цикл `while`, который рассчитывает, через сколько секунд объём впервые достигнет или превысит предел. Программа должна вывести время и итоговый объём; проверьте, что это 10 с и 1024 нм³.

---

### 6. Проверка сигналов (Цикл for и списки)

**Контекст:** Спектрометр выдал учебный список интенсивностей: `signals = [1.2, 0.5, 4.8, 0.2, 5.1, 3.0, 0.9]`. По условному правилу значения ниже 1.0 **помечают для проверки**, но не объявляют шумом только на основании порога.

**Задача:** Используя цикл `for`, создайте отдельный список записей со значением сигнала и статусом `"проверить"` или `"по учебному правилу допустимо"`. Выведите записи и исходный `signals`; он должен остаться неизменным. Сколько значений требуют проверки? Для настоящего спектра дополнительно понадобились бы длины волн, метод измерения фона и первичная выгрузка.

---

## 🧠 Блок 4: Комплексные задачи (Синтез знаний)

### 7. Маршрут сканирования (Вложенные циклы)

**Контекст:** Для учебного маршрута сканирования по сетке $3 \times 3$ нужно перечислить координаты. Сами координаты не моделируют туннельный ток или восстановление рельефа поверхности.

**Задача:** Используя вложенные циклы, перечислите координаты $X$ и $Y$ (каждая принимает значения от 1 до 3). Программа должна вывести строки вида:

```
Сканирую точку: X=1, Y=1
Сканирую точку: X=1, Y=2
...
```

и так далее для всех 9 точек.

---

### 8. Аварийная остановка сборки (Операторы break / continue)

**Контекст:** Робот собирает ДНК-оригами (наноконструктор). Он последовательно обрабатывает список команд: `commands = ["собрать", "собрать", "пропустить", "собрать", "дефект", "собрать"]`.

**Задача:** Напишите цикл, который обрабатывает этот список:

- Если команда `"пропустить"` — робот должен перешагнуть её (использовать `continue`) и вывести `"Пропуск шага"`.
- Если команда `"дефект"` — сборка должна немедленно остановиться (использовать `break`), а программа — вывести `"Авария! Обнаружен дефект конструкции"`.
- Для остальных команд выводить `"Элемент успешно установлен"`.

---

## 📚 Дополнительные задания

### 9. Паспорт наноматериала (Словари)

**Контекст:** В учебном каталоге сведения об образце слоистого углеродного материала хранятся в виде словаря.

**Задача:** Создайте словарь `nanomaterial`, содержащий следующие пары ключ-значение:

- `"name": "Слоистый углеродный образец"`
- `"layer_count": 2` (количество слоёв в исследуемом образце; это не число аллотропных форм)
- `"conductivity": "Высокая"`

Напишите код, который:

1. Изменяет количество слоёв (`layer_count`) на 3.
2. Добавляет новый ключ `"formula"` со значением `"C"`.
3. Безопасно проверяет (через `.get()`), есть ли в словаре ключ `"melting_point"`. Если ключа нет, пусть программа выводит: `"Данные о температуре плавления отсутствуют"`.

---

### 10. Сравнение номинальных составов (Множества)

**Контекст:** Для двух учебных рецептур перечислены **запланированные** химические элементы: `part1 = {"Cd", "Se", "Zn", "S"}` и `part2 = {"In", "P", "Zn", "S"}`. Из этих множеств нельзя установить реальные примеси в полученных образцах.

**Задача:** Используя операции над множествами, найдите и выведите на экран:

1. Элементы, которые присутствуют и в первой, и во второй партиях (пересечение).
2. Полный уникальный список элементов, задействованных в обоих экспериментах (объединение).
3. Элементы, которые есть в первой партии, но отсутствуют во второй (разность).
