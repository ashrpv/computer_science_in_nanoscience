# Домашнее задание. Блоки, партии и случайные эффекты

Практика к [лекции 7](07-random_effects_and_blocks.md). Задачи следуют логике постановки опыта: что рандомизируется, какую вариацию нужно сохранить, как анализировать данные и на что распространяется вывод. Числа основного варианта искусственные; реальный набор для проектного продолжения указан ниже. Формулы ANOVA допустимо использовать без повторения доказательств, но обязательно указывать правильный уровень независимости.

**Маршрут работы.** Выполните задачи 1–7 для основного варианта; реология, ПЭМ, STA и дифрактограммы ниже — самостоятельные продолжения по выбору. Для расчёта достаточно Python с NumPy, pandas, SciPy и statsmodels из [окружения курса](../requirements.txt). Сначала изобразите структуру «день → новая партия → приборное измерение», затем нарисуйте рандомизированный план, рассчитайте величины в единицах отклика и только после этого задайте модель. Рядом с каждым числом `n` подписывайте, что именно оно считает.

## 🧪 Блок 1. Блоки и сравнение рецептур

### 1. Три состава в четыре дня

**Контекст:** Три рецептуры твёрдых липидных наночастиц A, B, C готовили в каждый из четырёх дней. В каждой ячейке — **отдельно приготовленная партия**. Отклик — доля сохранённого активного вещества через 7 суток, %:

| День | A | B | C |
|---|---:|---:|---:|
| 1 | 91 | 84 | 87 |
| 2 | 95 | 88 | 90 |
| 3 | 102 | 92 | 96 |
| 4 | 89 | 81 | 85 |

**Задача:** Найдите несоответствие физических границ отклика и укажите, как запросить первичную запись для значения 102, не исправляя его молча. Для демонстрации методики продолжите расчёт с числом 102, явно пометив результат условным. Сравните средние A и B и четыре разности A−B внутри дней; объясните, что контролирует блок «день». Запишите модель `y = рецептура + день + ошибка` и назовите предпосылку, нужную для переноса различий между рецептурами на другие дни.

Различайте процентные **пункты** при вычитании двух процентов и относительное изменение в процентах. Постройте рисунок из четырёх пар A/B с линиями **внутри** дня; точку `102 %` выделите как ожидающую проверки. Запишите два возможных исхода аудита: первичная запись подтверждает число либо выявляет ошибку ввода. В обоих случаях сохраняйте исходную запись и новое решение в журнале QC. Не используйте условный интервал или p-value как подтверждение эффекта до разрешения этой записи.

### 2. Рандомизация внутри блока

**Контекст:** За день можно приготовить ровно по одной партии каждого состава; время суток влияет на вязкость исходного сырья.

**Задача:** Сгенерируйте с фиксированным seed отдельную случайную перестановку A/B/C на каждый из четырёх дней и представьте её в таблице. Что нарушится, если всегда готовить A утром, B днём, C вечером? Почему нельзя переставить дни целиком после получения результатов и считать это рандомизацией?

Назначьте `batch_id` каждой новой партии и `preparation_order` внутри дня. Проверьте программно, что в каждом из четырёх дней встречаются A, B и C ровно по одному разу и что ни один `batch_id` не повторяется. Сохраните seed и первоначальную таблицу назначений **до** появления откликов; порядок съёмки прибора при необходимости рандомизируют и документируют отдельно.

### 3. Технические повторы и псевдорепликация

**Контекст:** Для каждой из 12 партий дважды измерили долю активного вещества. Разница между техническими отсчётами обычно около 1 процентного пункта.

**Задача:** Составьте схему таблицы с `day_id`, `recipe`, `batch_id`, `measurement_id`, `retained_pct`. Укажите числа уровней по дизайну: дни, партии, измерения. Предложите два способа анализа (среднее по партии; модель с вложенными измерениями), объясните, почему считать 24 измерения независимыми и сравнивать группы обычным t-тестом неверно.

В схеме проверьте однозначность `measurement_id → batch_id` и `batch_id → (day_id, recipe)`; если ожидается ровно два отсчёта для каждой из 12 партий, пропуск одного отсчёта отмечайте как пропуск, а не подменяйте новым независимым приготовлением. Для агрегирования сначала определите правило обработки сомнительного `102 %`, потом укажите число партий на рецептуру. При смешанной модели объясните, какая часть ошибки относится к партии, а какая — к техническим измерениям.

## 📊 Блок 2. Вариация между партиями

### 4. Внутриклассовая корреляция

**Контекст:** Для микропористого полимера оценили дисперсию между партиями `σ²_batch = 16 (мг/г)²` и остаточную дисперсию повторных определений `σ²_error = 9 (мг/г)²`.

**Задача:** Рассчитайте общую дисперсию и ICC; поясните смысл ICC в этой задаче. Найдите дисперсию среднего из трёх измерений **одной** партии и дисперсию среднего из трёх **независимых** партий, если каждую измерили один раз. На основании вычислений решите, что полезнее для оценки среднего технологии при ограничении в три приборных запуска; оговорите стоимость синтеза.

Начните с `y_ij = μ + u_i + e_ij` и обозначьте, какие слагаемые остаются общими у трёх измерений той же партии. В первом случае член `σ²_batch` **не** делится на три; во втором делятся оба компонента. Единица всех дисперсий — `(мг/г)²`, а ICC безразмерен. Сравните математическую точность с практическими затратами: условие «три приборных запуска» не означает, что новые синтезы бесплатны.

### 5. Вложенные и скрещённые факторы

**Контекст:** Три оператора измеряют частицы из каждой партии оксида железа; сначала операторов распределяли так, что каждый работал только со «своей» партией. Затем протокол изменили: каждый оператор стал измерять каждую партию.

**Задача:** Нарисуйте две схемы «партия — оператор — измерение», назовите отношения вложенности и скрещивания. Какая версия позволяет отделить вклад оператора от вклада партии? Предложите случайные и фиксированные эффекты для сравнения двух методов синтеза с обобщением на новые партии. Если модель плохо оценивает дисперсию при малом числе партий, как это отразить в выводе?

### 6. Планирование мощности на уровне партий

**Контекст:** Для сравнения двух технологий средняя разница, важная практически, — 8 нм; ожидаемое стандартное отклонение **между партиями** — 10 нм. Два технических измерения каждой партии дешевле одного нового синтеза.

**Задача:** Рассчитайте ориентировочное число независимых партий в каждой группе для двухстороннего сравнения при α = 0.05 и мощности 0.80, используя `statsmodels` либо симуляцию с фиксированным seed. Напишите, какую модель распределения и равенства дисперсий вы приняли. Для симуляции включите уровень партии и измерительный шум; сравните мощность при добавлении партий и при добавлении технических повторов. Не подставляйте количество измерений вместо количества партий в формулу для независимых групп.

Для однозначного учебного расчёта примите независимые нормальные эффекты партий со стандартным отклонением `10 нм` и независимую ошибку **каждого** технического измерения со стандартным отклонением `4 нм`, одинаковые в обеих технологиях. При двух отсчётах на партию разброс **среднего партии** равен `sqrt(10² + 4²/2)` нм: передайте `8 / sqrt(10² + 4²/2)` как standardized effect в `statsmodels.stats.power.TTestIndPower.solve_power`, затем округлите возвращаемое `nobs1` вверх как число партий **в каждой группе**. При симуляции генерируйте новый эффект для каждой партии и новые ошибки для каждого отсчёта, усредняйте внутри партии и применяйте двусторонний тест к **средним партий**. Сравните сценарии `(n_batch, n_technical) = (10, 2), (20, 2), (20, 4)`; число прогонов симуляции, seed и оценочную погрешность мощности укажите в отчёте. Если симуляция и аналитическая оценка различаются, обсудите конечное число прогонов и фактическую модель, а не подгоняйте ответ.

### 7. Выбор блоков для своей лаборатории

**Контекст:** В синтезе опалов может меняться партия исходных сфер, при измерении фотолюминесценции — сеанс прибора, при выщелачивании — партия металла.

**Задача:** Выберите свой отклик и предложите полный рандомизированный блочный план на две технологии и хотя бы три блока: что считается блоком, какие обработки возможны в каждом блоке, в каком порядке назначаются обработки и какая вариация остаётся неучтённой. Представьте пустой лист регистрации результатов и объясните, как поступить, если одно измерение в блоке окажется недоступным.

Для пустого листа используйте поля `block_id, batch_id, treatment, preparation_order, measurement_id, response, response_unit, qc_status`. Каждая технология должна появиться в каждом блоке на **своей** независимо приготовленной партии; три блока — учебный минимум, не доказательство достаточной мощности. Объясните, что происходит, когда пропала только техническая запись и когда целая партия не дала отклика: эти два случая по-разному влияют на возможность парного сравнения. Запретите автоматическую замену пропуска средним блока.

## Самостоятельная работа: проверка границ вывода

Возьмите **один** результат из задач 1, 4 или 6 и подготовьте небольшой «аудит интерпретации» для коллеги: исходные ID и единицы, уровень независимости, расчёт вручную, проверка кодом, допустимое предложение для отчёта и пример слишком сильного вывода, который эти данные не поддерживают. Для задачи 1 сравните условный результат при исходном `102 %` с **сценарием** исправления только после подтверждения первичного файла; никогда не заменяйте наблюдение догадкой. Для задачи 4 сравните стандартные ошибки двух способов распределить три запуска. Для задачи 6 покажите на графике мощность от числа партий для двух чисел технических отсчётов, чётко подписав принятые дисперсии и модель. Эти варианты расширяют материал слайдов, но опираются на тот же принцип независимого назначения воздействия.

**Самопроверка:** задачи 1–3 описывают четыре дня, 12 разных партий и, если каждый запуск выполнен, 24 технических измерения; четыре внутридневные разности не становятся 12 независимыми контрастами. В задаче 4 три отсчёта одной партии сохраняют общую межпартийную дисперсию. В задаче 6 размер группы измеряется партиями, и при неизменном числе партий снижение приборной ошибки техническими повторами не устраняет разброс приготовления. В задаче 7 без отклика одной из партий блока нельзя молча считать пару полной.

## Продолжение реологического мини-проекта

**Контекст:** В [опубликованной серии наноэмульсии](../experimental_analysis/data/rheology/README.md) шесть разных образцов происходят из **одной партии**. Измерения выполнены при одинаковой обработке и термостатировании, в одной геометрии «цилиндр в цилиндре», но на точках одной кривой и даже на шести образцах нельзя оценить вариацию между независимо приготовленными партиями. Уровень 0 относится к другому сеансу, а два шага при уровнях 0,25 и 0,75 % измерены на разных образцах.

**Задача:** Спроектируйте проверяющий опыт для четырёх уровней состава и двух шагов программы. В качестве начального варианта рассмотрите три **независимых приготовления на уровень** (12 партий) и два отдельных образца из каждой партии для двух шагов (24 измерения); это пример структуры, а не обоснование достаточной мощности. Назначение уровня происходит при приготовлении партии, шага — отдельному образцу. Разместите уровни в каждом дне или явно объясните, какие сравнения будут смешаны с днём. Составьте рандомизированный порядок приготовления внутри дня и измерения образцов, опишите одинаковые подготовку, предварительный сдвиг, термостатирование и QC. Укажите, когда вы считаете пару шагов связанной по партии и почему 200 точек каждого запуска не увеличивают число партий.

Выберите **до нового опыта** первичный отклик, например кажущуюся вязкость при 101,1 с⁻¹ в заданном направлении, правило приёмки и метрику различия шагов. Предложите способ обоснования числа партий на основе пилотной оценки межпартийного разброса либо расчёта мощности; текущий набор одной партии такой оценки не содержит. Сдайте пустую таблицу `day_id, batch_id, specimen_id, run_id, level_pct, step_s, session_order, eta_Pa_s, qc_status` и покажите проверку её ключей.

## Дополнительный вход в мини-проект ПЭМ

В [серии магнетита](../experimental_analysis/data/tem_magnetite/README.md) 20 включённых вариантов обозначают 20 **отдельных синтезов**, а 90 снимков и 6625 частиц вложены в эти образцы. Выполните [этап 3 мини-проекта](../experimental_analysis/09-tem-homework.md#этап-3-вложенная-вариация-и-следующий-опыт-лекция-7): сравните условную неопределённость при перевыборке частиц внутри фиксированных снимков и перевыборку независимых образцов целиком, сохраняя один и тот же оцениватель. При желании оцените вклад уровней `sample_id` и `image_id` в описательной смешанной модели.

Режимы синтеза и индивидуальная форма частиц неизвестны. Поэтому модель описывает разброс *записанного линейного размера силуэта*, а не эффект конкретной рецептуры или долю кубических частиц. Придумайте дизайн следующего опыта с метками технологических групп, заранее определённым признаком, новым синтезом для каждого независимого повтора и планом выбора полей зрения.

## Что сдать

Для основного варианта — расчётный notebook или PDF с таблицей рандомизации, ручной проверкой хотя бы одного контраста, кодом для ICC и мощности, схемами вложенности/скрещивания и протоколом задачи 7. Укажите, что значение 102 % требует проверки исходных данных, а условный расчёт по нему не является научным результатом.

Для реологического продолжения — протокол, пустая таблица регистрации, рандомизированный план и объяснение независимой единицы, блоков и числа партий.

**Критерии проверки основного варианта:** правильная иерархия единиц и ключей, решение по QC — 30 %; расчёты контрастов, ICC и мощности с единицами и проверкой — 30 %; рандомизация, выполнимый блочный протокол и обработка пропусков — 25 %; ясные предпосылки и границы обобщения — 15 %. Для любого самостоятельного продолжения те же критерии применяются к структуре фактически доступных измерений; не объявляйте множество точек кривой или частиц независимыми приготовлениями.


## Дополнительный вариант: блоки STA

Для [серии STA](../datasets/sta_iron_oxides/README.md) предложите блокированный проспективный план «с матрицей / без матрицы × три скорости» в трёх независимых блоках приготовления. Изобразите вложенность блок → отдельно приготовленный образец → прогон → отсчёты; укажите рандомизацию, холостые опыты и структурные измерения. Сопоставьте с фактической картой 198/200/201/202 и 105–108: существующие измерения не образуют сбалансированного парного эксперимента. Три блока — пример, не результат расчёта мощности.

## Дополнительный вариант: план для дифрактограмм

По [серии Fe–SiO₂](../datasets/xrd_fe_silica/README.md) составьте карту факторов: для железосодержащих образцов есть по одному образцу в четырёх ячейках 93/138 нм × 1000/1100 °C; отдельно измерены два независимых образца 120 нм / 1000 °C, контроль SiO₂ при 120 нм / 1000 °C и контроль при 138 нм / 1100 °C. Используйте результаты [практикума к лекции 8](../experimental_analysis/08-xrd-homework.md#4-сравнение-факторов-и-следующий-опыт) как пилотные признаки, но не подменяйте отсутствие повторов множеством точек дифрактограммы и не оценивайте дисперсию каждой факторной ячейки по одному образцу.

Предложите новый план с независимыми приготовлениями в каждой нужной ячейке, контролями SiO₂ при сопоставимых режимах и повторными сканами части **тех же** образцов для оценки вариации измерения. Заранее зафиксируйте первичный признак, правила QC, случайный порядок приготовления/съёмки и блоки дня или партии исходных сфер. Отдельно опишите проверку расхождения 3911/3913: повторная съёмка различает приборный и образцовый вклад лишь частично; новыми приготовлениями проверяют вариацию синтеза. Представьте пустую таблицу `block_id, sample_id, matrix_diameter_nm, annealing_C, iron_present, scan_id, scan_order, feature_33, qc_status` и объясните уровень каждого фактора.
