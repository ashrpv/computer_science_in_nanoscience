# Домашнее задание. Спектры и экспериментальные кривые

Практика к [лекции 8](08-spectra_and_curves.md). Основной вариант: выполните задачи 1–6 на **искусственных** данных, созданных следующим кодом, или на документированных собственных измерениях. Дополнительно доступны практикумы на реальных [термоаналитических](08-sta-homework.md) и [рентгенодифракционных](08-xrd-homework.md) кривых; вариант на реологических кривых приведён ниже. Не выдавайте искусственную кривую за приборную. Для собственного набора сохраняйте исходный файл и параметры обработки отдельно.

Работайте из корня репозитория с [зависимостями курса](../requirements.txt). Задача 7 — самостоятельное продолжение по своей теме. Сначала запишите в notebook единицы и правило обработки, затем изменяйте параметры и документируйте эффект. Исходные массивы не перезаписывайте: для каждого варианта сохраните метод фона, окно, границы интегрирования и проверку качества. Анализ реального набора вязкости — отдельная альтернатива задачам 1–6 с тем же принципом сдачи воспроизводимого кода.

```python
import numpy as np

rng = np.random.default_rng(731)
wl_nm = np.arange(480., 681., 1.)
baseline = 0.06 + 0.0003 * (wl_nm - 480)
peak_1 = 1.2 * np.exp(-0.5 * ((wl_nm - 530) / 9)**2)
peak_2 = 0.6 * np.exp(-0.5 * ((wl_nm - 575) / 13)**2)
signal = baseline + peak_1 + peak_2 + rng.normal(0, 0.015, wl_nm.size)
blank = baseline + rng.normal(0, 0.005, wl_nm.size)
```

Условный образец — перовскитные квантовые точки в полимерной матрице; интенсивность дана в относительных единицах. Два пика добавлены для учебной проверки метода, но на реальных данных число компонент нельзя определять только по тому, как был написан генератор.

## 🔬 Блок 1. Получение признаков

### 1. Приёмка файла и оси

**Контекст:** При экспорте спектрометра возможны повторяющиеся длины волн, пустые сигналы и насыщение.

**Задача:** Соберите таблицу `wl_nm`, `signal`, `blank`, добавьте проверки числовых типов, возрастающей оси, дубликатов, пропусков и конечности значений. Выведите шаг сетки и его диапазон. Контроль для данного генератора: 201 значение длины волны от 480 до 680 нм включительно с шагом 1 нм. Создайте отдельную копию с испорченным элементом оси и покажите, что проверка его обнаруживает, не изменяя исходный набор.

### 2. Коррекция фона

**Контекст:** `blank` измерен отдельно и содержит собственный шум.

**Задача:** Постройте исходный сигнал, холостой опыт и разность. Сравните для пика около 530 нм площадь над фоном по двум заранее записанным методам: (i) вычитание `blank`, (ii) оценка линейного фона по краям 480–495 и 645–680 нм. Интегрируйте на одинаковом интервале 505–552 нм методом трапеций **по фактической оси**; укажите единицы площади (отн. ед.·нм) и объясните, почему результаты методов могут различаться. Не называйте `blank` безошибочным фоном: шум холостого опыта тоже попадёт в разность.

### 3. Сглаживание и максимум

**Контекст:** Для оценки положения пика нужен устойчивый к шуму максимум.

**Задача:** Примените `savgol_filter` с `polyorder=3` и окнами 7, 15, 31 точка к скорректированному сигналу. Для каждого окна найдите дискретное положение и высоту пика в диапазоне 505–552 нм. Постройте наложение исходной и обработанных кривых; сравните смещения. Объясните, почему подбор окна по «красоте» графика не является научным критерием.

### 4. Ширина и наложение пиков

**Контекст:** У спектра есть узкая полоса около 530 нм и более широкая около 575 нм.

**Задача:** Найдите пики с явно заданным `prominence` и оцените ширину на половине высоты **относительно выбранного определения фона**. Преобразуйте ширину из отсчётов в нм. Если используете `scipy.signal.peak_widths(..., rel_height=0.5)`, поясните, что уровень здесь задан относительно **prominence**, поэтому возвращаемая ширина не обязана совпадать с FWHM относительно нулевого фона. Попробуйте фитировать одну и две гауссовы компоненты на интервале 505–620 нм; сравните остатки на общем графике и запишите, что физически означают параметры. Для контроля порядка величины из генератора отдельная первая компонента без шума имеет FWHM около 21,2 нм (`2√(2 ln 2) · 9 нм`); смешанный зашумлённый сигнал не обязан дать это значение. Не объявляйте две компоненты доказательством двух фаз без независимого подтверждения.

## 📈 Блок 2. Другая кривая и контроль результата

### 5. Кинетика выщелачивания меди

**Контекст:** В учебном опыте с обратной микроэмульсией измерена концентрация Cu в растворе через `[0, 5, 10, 20, 40, 60]` мин: `[0.0, 0.8, 1.5, 2.5, 3.4, 3.8]` ммоль/л. Объём раствора постоянен, 20 мл.

**Задача:** Постройте исходную кинетику и рассчитайте массу растворённой меди на каждом шаге по выбранной молярной массе, которую укажите. При постоянном объёме `m_Cu,мг = C_Cu,ммоль/л · 0,020 л · M_Cu,г/моль`; для последней точки и `M_Cu ≈ 63,5 г/моль` получится около 4,83 мг. Сравните линейную интерполяцию и модель насыщения `C(t) = C_inf * (1 - exp(-k*t))` **на отложенной внутренней точке**, например временно исключите 20 мин и предскажите её по оставшимся точкам. На узлах интерполяция по определению имеет нулевые остатки: сравнивать с ними остатки подогнанной модели как показатель превосходства некорректно. Покажите остатки модели на всех обучающих точках, оцените `C_inf` и `k` с единицами (для `k` — мин⁻¹) и обсудите, насколько вывод зависит от малого числа временных точек. Какие данные о потерях раствора, температуре и исходной массе металла нужны до расчёта степени выщелачивания? Не называйте коэффициент `k` константой элементарной реакции без обоснования модели.

### 6. Неопределённость и QC серии

**Контекст:** Форма спектра меняется при иной коррекции фона и сглаживании.

**Задача:** Составьте сетку минимум из двух способов фона и трёх окон задачи 3. Для каждого сочетания получите положение максимума и площадь пика, сохраните параметры в таблицу. Отдельно покажите разброс от обработки и, если есть независимые спектры, разброс между образцами; эти две величины нельзя смешивать в одно `n`. Предложите три критерия браковки спектра до взгляда на значения целевых пиков.

### 7. Переход к своей научной задаче

**Контекст:** Подход годится также для STA-кривой, фотолюминесценции, реологии лецитиновых систем и кривой сорбции.

**Задача:** Выберите один свой тип кривой или используйте данные задачи 5. Сформулируйте целевой признак, его единицу, допустимый диапазон, контроль и один источник систематической ошибки. Составьте короткий протокол обработки с фиксированными правилами для серии файлов и укажите, какой новый независимый опыт проверит интерпретацию признака.

## Вариант на реальных данных: реологические кривые

Вместо задач 1–6 можно выполнить сквозной мини-проект по [обезличенной серии наноэмульсии](data/rheology/README.md): [широкая таблица измерений](data/rheology/viscosity_wide.csv) и [реестр образцов](data/rheology/specimens.csv). Все шесть запусков сделаны на отдельных образцах **одной партии** в геометрии «цилиндр в цилиндре», при одинаковой обработке и термостатировании. Два сеанса обозначены A и B. По 100 точек измерены при увеличении скорости сдвига и ещё 100 — при уменьшении; при уровнях 0,25 и 0,75 % есть отдельные образцы с шагом 3 и 6 с.

### 8. Таблица и контроль качества

Преобразуйте широкий экспорт в длинную таблицу с ключом `run_id × sweep × point_no` и исходной строкой. Присоедините `specimen_id`, `batch_id`, `formulation_id`, `level_pct`, `step_s` и `session_id` через реестр. Проверьте число строк 6 × 2 × 100, единицы, порядок скорости, монотонность общего времени и сброс времени сегмента. Пересчитайте `τ/γ̇` и сравните с записанной `η` с учётом округления. Выведите таблицу QC, сохраняя последнее нулевое показание `R01` в данных; сформулируйте, какую первичную запись надо запросить, прежде чем менять статус точки.

### 9. Два направления измерения

На общих осях покажите `τ(γ̇)` и `η(γ̇)` с различимыми уровнем состава, направлением хода и шагом. Подберите линейные и логарифмические представления так, чтобы не потерять нулевое значение `η` при построении логарифма: отсутствие точки **на этом графике** не означает удаление строки. Подпишите единицы.

Для каждого запуска найдите вязкость на обоих ходах при 101,1 и 505,1 с⁻¹; рассчитайте `η_down − η_up` и относительную разность с явно заданным знаменателем. Сравните знаки и величины по сериям. Отдельно сопоставьте начальную и конечную точки около 0,1 с⁻¹ и покажите, как нулевое показание `R01` ограничивает интерпретацию именно этого сравнения. Что из наблюдений может зависеть от времени на точку или истории сдвига, а что не отделяется от разницы между образцами?

### 10. Модель в ограниченном диапазоне

На заранее объявленном интервале 30–900 с⁻¹ отдельно для прямого и обратного ходов подгоните `τ = Kγ̇ⁿ`, `η = Kγ̇⁽ⁿ⁻¹⁾`. Укажите единицы `K` (Па·сⁿ), способ подгонки и остатки в исходных единицах напряжения; сопоставьте с моделью постоянной вязкости на том же интервале. Повторите оценку `n` при другом разумном интервале и покажите изменение. Не называйте степенную зависимость законом на всём диапазоне и не считайте `τ` и полученную из него `η` независимыми откликами.

### 11. Область вывода и следующий опыт

Сопоставьте пары уровней 0,25 и 0,75 % при двух шагах и явно укажите, что это **разные образцы одной партии**. Уровень 0 получен в другом сеансе, поэтому различие с остальными уровнями также смешано с сеансом. Предложите опыт с новыми независимо приготовленными партиями, случайным порядком запусков и заданным заранее откликом при фиксированной скорости. Для него можно воспользоваться [вариантом к лекции 7](../experimental_design/07-homework.md#продолжение-реологического-мини-проекта).

**Что сдать для этого варианта:** выполняемый notebook или скрипт, таблицу с происхождением точек и журнал QC, две читаемые группы кривых, таблицу разностей, рисунок остатков, ограничения интерпретации и короткий план нового опыта. **Оценивание:** происхождение и ключи — 20 %; QC — 20 %; графики и сравнение ходов — 20 %; модель и чувствительность к окну — 20 %; ограничения и план — 20 %.

## Что сдать

Для основного варианта — запускаемый сверху вниз код, сохранённый рисунок со спектром/фоном/остатками, таблицу признаков и параметров обработки (единицы, интервалы, окно, правило поиска пика), рисунок кинетики с отложенной точкой и текстовое описание QC. Для данных своей лаборатории приложите обезличенный пример либо схему входных полей, если передавать исходные файлы нельзя. Укажите, какие результаты относятся к модельной кривой и какие — к измерению.

**Самопроверка основного варианта:** ось содержит 201 точку; обе оценки площади используют один и тот же диапазон; ширина подписана в нм и с указанным определением высоты; масса меди последней точки имеет порядок нескольких миллиграммов; ни один перебор фона не назван независимым экспериментом.

**Оценивание основного варианта:** приёмка и происхождение данных — 20 %; фон и признаки с единицами — 30 %; кинетика и честное сопоставление моделей — 20 %; чувствительность и QC — 20 %; интерпретация и пределы вывода — 10 %.
